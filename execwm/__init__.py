"""ExecWM — a grounded latent world model of computation.

M0 layer (this commit): the substrate (DSL + bytecode VM + tracer), the
out-of-distribution generators, and the lossless state codec. These provide free,
exact ground truth for everything downstream.
"""

__version__ = "0.0.1"
